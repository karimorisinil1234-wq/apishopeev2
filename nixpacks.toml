providers = ["python"]

[phases.setup]
nixPkgs = ["python312", "python312Packages.pip", "chromium"]

[phases.install]
cmds = [
  "pip3 install --no-cache-dir -r artifacts/shopee-checker/requirements.txt"
]

[variables]
PLAYWRIGHT_BROWSERS_PATH = "0"
PLAYWRIGHT_CHROMIUM_EXECUTABLE_PATH = "/run/current-system/sw/bin/chromium"

[start]
cmd = "cd artifacts/shopee-checker && uvicorn main:app --host 0.0.0.0 --port $PORT --workers 1"
