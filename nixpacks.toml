providers = ["python"]

[phases.setup]
nixPkgs = ["python312", "chromium"]

[phases.install]
cmds = [
  "pip install -r artifacts/shopee-checker/requirements.txt"
]

[start]
cmd = "cd artifacts/shopee-checker && uvicorn main:app --host 0.0.0.0 --port $PORT --workers 1"
